<!DOCTYPE html>
<html>
  <head>
    <title>Onliner-clone</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css" rel="stylesheet">
  </head>
  <body>
    <div class="page">
      <div class="advt"><%= image_tag("cat.gif", size: '1400x90') %></div>
      <div class="top-menu">
        <div class="catalog">
          <span>Каталог</span>
          <span>Новости</span>
          <span>Автобарахолка</span>
          <span>Дома и квартиры</span>
          <span>Услуги</span>
          <span>Барахолка</span>
          <span>Форум</span>
        </div>
      </div>
      <div class="top-bar">
        <div class="bar1">
          <%= link_to((image_tag "https://gc.onliner.by/images/logo/onliner_logo.v3.png?token=1498651481", size: '180x40'), posts_path) %>
        </div>

        <div class="box">
          <div class="container-1">
            <span class="icon"><i class="fa fa-search"></i></span>
            <input type="search" id="search" placeholder="Поиск в Каталоге. Например, 'паркет tarkett'" />
          </div>
        </div>

        <div class="input">
          <% if current_user %>
            <div class="user avatar">
              <% if current_user.avatar %>
                <%= link_to((image_tag "/avatars/#{current_user.id}/avatar#{ current_user.id }.png", size: '60x60'), user_path(current_user.id)) %>
              <% else %>
                <%= link_to((image_tag url_for('https://content.onliner.by/user/avatar/60x60/197713'), width: '60', style: "background-color: #ccc; border: 1px solid #ccc;"), user_path(current_user.id)) %>
              <% end %>
            </div>

            <div class="user user-name nav-item dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"> </a>
              <div class="dropdown-menu">
                <span class="dropdown-item nickname"><%= current_user.name %></span>
                <% if current_user && current_user.admin? %>
                  <%= link_to 'Новая статья', new_post_path, class: "dropdown-item" %>
                  <%= link_to 'Список пользователей', users_path, class: "dropdown-item" %>
                  <%= link_to 'Статьи', list_path, class: "dropdown-item" %>
                <% end %>
                <div class="dropdown-divider"></div>
                <%= link_to 'Выйти', logout_path, class: "dropdown-item" %>
              </div>
            </div>

          <% else %>
            <div class="user-name">
              <div class="input-button">
              <span><%= link_to 'Вход', new_sessions_path, class: 'btn btn-outline-secondary' %></span>
              </div>
            </div>

          <% end %>
        </div>
      </div>

      <%= yield %>

      <div class="footer">
        <span>Все права защищены</span>
      </div>
    </div>
  </body>
</html>
