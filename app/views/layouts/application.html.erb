<!DOCTYPE html>
<html>
  <head>
    <title>MyBlog</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css" rel="stylesheet">
  </head>
  <body class="page">
    <div class="advt"><%= image_tag("cat.gif", size: '1400x90') %></div>
    <div class="top-menu">
      <span>Каталог</span>
      <span>Новости</span>
      <span>Автобарахолка</span>
      <span>Дома и квартиры</span>
      <span>Услуги</span>
      <span>Барахолка</span>
      <span>Форум</span>
    </div>
    <div class="top-bar">
      <div class="bar1">
        <%= link_to((image_tag "https://gc.onliner.by/images/logo/onliner_logo.v3.png?token=1498651481", size: '180x40'), posts_path) %>
      </div>

      <div class="box">
        <div class="container-1">
          <span class="icon"><i class="fa fa-search"></i></span>
          <input type="search" id="search" placeholder="Поиск в Каталоге. Например, 'паркет tarkett'" />
        </div>
      </div>

      <div class="input">
        <% if current_user %>
          <div class="user avatar">
            <% if current_user.avatar %>
              <%= link_to((image_tag "/avatars/#{current_user.id}/avatar#{ current_user.id }.png", width: '60'), user_path(current_user.id)) %>
            <% else %>
              <%= link_to((image_tag url_for('https://content.onliner.by/user/avatar/60x60/197713'), width: '60'), user_path(current_user.id)) %>
            <% end %>
          </div>
          <div class="user user-name"><%= current_user.name %>
            <div class="output"><%= link_to 'Выйти', logout_path %></div>
        <% else %>
            <div class="input-button">
<!--               <span><%= link_to 'Регистрация', new_user_path, class: 'btn btn-outline-secondary' %></span>
 -->              <span><%= link_to 'Вход', new_sessions_path, class: 'btn btn-outline-secondary' %></span>
            </div>
          </div>
        <% end %>
      </div>
    </div>

<!--     <nav class="navbar navbar-toggleable-md navbar-light bg-faded">
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <%= link_to((image_tag "https://gc.onliner.by/images/logo/onliner_logo.v3.png?token=1498651481", size: '180x40'), posts_path) %>
          </li>
          <% if current_user && current_user.name == 'admin' %>
            <li class="nav-item">
              <%= link_to 'Новая статья', new_post_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to 'Список пользователей', users_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to 'Статьи', list_path, class: "nav-link" %>
            </li>
          <% end %>
        </ul>
        <div class="input">
          <% if current_user %>
            <div class="user avatar">
              <% if current_user.avatar %>
                <%= link_to((image_tag "/avatars/#{current_user.id}/avatar#{ current_user.id }.png", width: '45'), user_path(current_user.id)) %>
              <% else %>
                <%= link_to((image_tag 'images.png', width: '45'), user_path(current_user.id)) %>
              <% end %>
            </div>

            <div class="user user-name"><%= current_user.name %>
              <div class="output"><%= link_to 'Выйти', logout_path %></div>
          <% else %>
              <div class="input-button">
                <span><%= link_to 'Регистрация', new_user_path, class: 'btn btn-success' %></span>
                <span><%= link_to 'Войти', new_sessions_path, class: 'btn btn-outline-success' %></span>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </nav>
 -->
    <%= yield %>

    <div class="footer">
      <span>Все права защищены</span>
    </div>

  </body>
</html>
